---
/**
 * 單一組件的CSS順序規則(正常情況):
 * 1. link匯入: 順序最前
 * 2. import匯入: 依照匯入順序，越後匯入順序越後
 * 3. <style>: 順序最後
 *
 * Astro 一個詭異的打包BUG:
 * 1. 沒有layouts:
 *   匯入組件的CSS -> 自身的CSS (正常)。
 * 2. 有layouts:
 *   2-1. layout無匯入外部CSS: [匯入組件 / 匯入佈局]的CSS -> 自身的CSS (正常)。
 *   2-2. layout匯入了外部CSS:
 *     **BUG**: 自身CSS打包後會處於一個無邏輯隨機性的位置。
 *   2-3. layout匯入了已匯入外部CSS的layout:
 *     **BUG**: 只匯入該layout時，順序毫無章法可言，再匯入一個或以上的組件後，最底層被匯入的外部CSS會回歸第一位，其餘的依然混亂排列，這就是這個Debug出現的原因。
 *   2-3. 同上，再套一次layout:
 *     **BUG**: 不只匯入了已匯入外部CSS的layout要套一個組件外，目前的layout還要再加套兩個不能相同的組件，才會讓最底層被匯入的外部CSS會回歸第一位。
 *   2-4. 同上，再套一次layout: 就沒救了。
 *
 * 結論:
 * 1. 不要用layout當作YongKingFuck的擴展佈局，要嘛就分區塊給頁面一個個匯入，每頁的底層確保始終是YongKingFuck。
 * 2. 每頁都要加這個Debug組件
 */
---

<slot />
