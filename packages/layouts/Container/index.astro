---
import { type ContainerProps, props as defaultProps } from './index.type'
import { containerStyleList, containerClassList } from './props'
import { mergeClasses } from '../../utils'
import '../../styles/container.scss'

interface Props extends ContainerProps {}

// 合併属性
const props = {
  ...defaultProps,
  ...Astro.props,
}
const { element, class: className, maxWidth, center, ...rest } = props

const Element = element as keyof HTMLElement
const styleList = containerStyleList(maxWidth)
const classList = mergeClasses(className, containerClassList(center))
---

<Element class:list={classList} style={styleList} {...rest}><slot /></Element>
