---
import Component from '@components/DEMO/Btn06.astro'
import config from '../config.json'
import content from '../../content/demo.json'

export const id = 'btn_type06'
export const { keywords, category } = config['components'][id]
export const props = {
  href: { type: ['string'], require: false, default: '#', demo: '#' },
  class: { type: ['string'], require: false, default: '' },
  color: { type: ['string'], require: false, default: 'inherit' },
  backgroundColor: { type: ['string'], require: false, default: '#eef1f6', demo: '#8886' },
  borderColor: { type: ['string'], require: false, default: '#fff0' },
  hoverBorderColor: { type: ['string'], require: false, default: '#00b39b' },
  blank: { type: ['boolean'], require: false, default: false },
}
export const slots = ['default', 'icon']
const data = content['button']?.[0] ?? {}
---

<Component href={props.href.demo} backgroundColor={props.backgroundColor.demo}>
  {data.text}
</Component>
